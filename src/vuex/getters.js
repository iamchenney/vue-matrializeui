
export function activePosition (state) {
  return state.activePosition
}

export function positions (state) {
  return state.positions
}

export function positionBatches (state) {
  return state.positionBatches
}

export function resumes (state) {
  return state.batchResumes
}

export function activeBatch (state) {
  return state.activeBatch
}
